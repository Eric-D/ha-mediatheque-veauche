{
  "name": "mediatheque-card-frontend",
  "version": "3.5.0",
  "description": "Lovelace card sources for ha-mediatheque-veauche",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "lit": "^3.2.1",
    "typescript": "^7.0.0"
  }
}
