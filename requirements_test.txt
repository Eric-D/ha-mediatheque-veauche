# Dépendances pour exécuter la suite de tests.
# Home Assistant n'y figure pas volontairement : conftest.py le simule, les
# tests ne portant que sur le scraping et les fonctions pures.
pytest>=8.0.0
beautifulsoup4>=4.12.0
requests>=2.28.0
